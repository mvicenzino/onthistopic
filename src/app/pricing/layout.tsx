export const metadata = {
  title: 'Pricing & Plans — Enterprise Leadership Development',
  description:
    'Transparent, per-participant annual pricing. No hidden fees. From $96 per participant per year.',
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
